import { LoggedMessage } from "./types"

export const parseRawLog = (rawLog: string): LoggedMessage => {
    const args = 
}

export const createLoggedEvent = (message: LoggedMessage): LoggedEvent => {

}